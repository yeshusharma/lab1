Version control tool. Uses different commands and unix envioronment on linux op which makes the connection & interaction between the local disk and the online enviroment simpler in case of fast transfer of the files for the user. Since GIT is an online hub, it also offers huge space which would allow the user to add plenty of files to the repository. One of the evdient features of GIT is that it allows any user to access files uploaded by another user and modify them in acse any changes are needed. The command also seem simple and easily understandable for the user to work on. 
